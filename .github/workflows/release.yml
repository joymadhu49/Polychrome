name: Release (signed + notarized)

# Cuts a downloadable, Gatekeeper-clean DMG — automatically.
#
# Triggers:
#   • push to main — if Bundle/Info.plist's CFBundleShortVersionString has no
#     matching v<version> tag yet, the release cuts itself: build, sign,
#     notarize, create the tag + GitHub Release, attach the DMG. Merging a PR
#     that bumps the version is the only human step. A push to main without a
#     version bump releases nothing.
#   • push of a v* tag — the classic manual flow, still supported.
#   • workflow_dispatch — optional release_tag / target_commitish override;
#     with no inputs it auto-detects the version like a push to main.
#
# Required repository secrets (Settings → Secrets and variables → Actions):
#   DEVELOPER_ID_CERT_P12        base64 of the exported "Developer ID Application" cert (.p12)
#   DEVELOPER_ID_CERT_PASSWORD   the password set when exporting that .p12
#   KEYCHAIN_PASSWORD            any random string (temp keychain password for the runner)
#   AC_API_KEY_P8                base64 of the App Store Connect API key (.p8)
#   AC_API_KEY_ID                the API Key ID
#   AC_API_ISSUER_ID             the API Issuer ID
#
# Create base64 secrets with:  base64 -i Certificates.p12 | pbcopy

on:
  push:
    tags: ['v*']
    branches: [main]
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Tag to release as (e.g. v1.4.2). Leave empty to auto-detect from Info.plist.'
        required: false
        default: ''
      target_commitish:
        description: 'Commit SHA (or branch) the new tag should point at. Defaults to the built commit.'
        required: false
        default: ''

permissions:
  contents: write   # create the tag, the GitHub Release, and upload the DMG asset

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  # Cheap gate on a Linux runner: figure out which tag (if any) this run
  # should release, so pushes to main without a version bump skip the
  # expensive macOS build entirely.
  check:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.ver.outputs.tag }}
    steps:
      - uses: actions/checkout@v4
      - name: Decide whether a release is due
        id: ver
        env:
          RELEASE_TAG: ${{ inputs.release_tag }}
        run: |
          set -euo pipefail
          TAG=""
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            TAG="$GITHUB_REF_NAME"
          elif [[ -n "${RELEASE_TAG:-}" ]]; then
            TAG="$RELEASE_TAG"
          else
            VER=$(python3 -c "import plistlib; print(plistlib.load(open('Bundle/Info.plist','rb'))['CFBundleShortVersionString'])")
            TAG="v$VER"
            if git ls-remote --exit-code --tags origin "refs/tags/$TAG" >/dev/null 2>&1; then
              echo "Tag $TAG already exists — nothing new to release."
              TAG=""
            fi
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "Release due: ${TAG:-no}"

  release:
    needs: check
    if: needs.check.outputs.tag != ''
    runs-on: macos-14
    env:
      POLYCHROME_SIGNING_IDENTITY: "Developer ID Application"
    steps:
      - uses: actions/checkout@v4

      - name: Import Developer ID certificate
        env:
          DEVELOPER_ID_CERT_P12: ${{ secrets.DEVELOPER_ID_CERT_P12 }}
          DEVELOPER_ID_CERT_PASSWORD: ${{ secrets.DEVELOPER_ID_CERT_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          set -euo pipefail
          KEYCHAIN="$RUNNER_TEMP/build.keychain-db"
          CERT="$RUNNER_TEMP/cert.p12"
          echo "$DEVELOPER_ID_CERT_P12" | base64 --decode > "$CERT"

          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security set-keychain-settings -lut 21600 "$KEYCHAIN"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security import "$CERT" -k "$KEYCHAIN" -P "$DEVELOPER_ID_CERT_PASSWORD" \
            -T /usr/bin/codesign -T /usr/bin/security
          # Allow codesign to use the imported key without an interactive prompt
          security set-key-partition-list -S apple-tool:,apple:,codesign: \
            -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN" >/dev/null
          # Put our keychain on the search list so codesign/find-identity can see it
          security list-keychains -d user -s "$KEYCHAIN" "$HOME/Library/Keychains/login.keychain-db"
          rm -f "$CERT"

          echo "Installed signing identities:"
          security find-identity -v -p codesigning "$KEYCHAIN" | grep "Developer ID Application"

      - name: Build + sign app
        run: bash scripts/build.sh

      - name: Create DMG
        env:
          TAG: ${{ needs.check.outputs.tag }}
        run: |
          set -euo pipefail
          bash scripts/make-dmg.sh "${TAG#v}"

      - name: Notarize + staple
        env:
          AC_API_KEY_P8: ${{ secrets.AC_API_KEY_P8 }}
          AC_API_KEY_ID: ${{ secrets.AC_API_KEY_ID }}
          AC_API_ISSUER_ID: ${{ secrets.AC_API_ISSUER_ID }}
        run: bash scripts/notarize.sh

      # Creates the tag (when it doesn't exist yet) and the GitHub Release, and
      # attaches the DMG. GITHUB_TOKEN-created tags don't re-trigger workflows,
      # so the auto flow can't recurse.
      - name: Publish the GitHub Release with the DMG
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.check.outputs.tag }}
          target_commitish: ${{ inputs.target_commitish || github.sha }}
          generate_release_notes: true
          files: build/Polychrome-*.dmg
          fail_on_unmatched_files: true
